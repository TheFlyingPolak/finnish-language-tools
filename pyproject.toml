[project]
name = "finnish-language-tools"
version = "0.0.1"
description = "A collection of Python scripts for Finnish language studies"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "libvoikko"
]
